import React from "react";
export interface IconProps {
    className?: string;
    style?: React.CSSProperties;
}
export interface IconData {
    tagname: string;
    icon: string;
    npm_tag: string;
}
